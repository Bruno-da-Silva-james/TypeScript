/*
//criar array
let arrayString: string[] = ['Meu nome é','Bruno James']
let arrayNum: number[] = [12,3]

//adicionar elementos ao final do array
arrayNum.push(14)

//mostrar o array
console.log(arrayNum);

//loopar as iteracoes e mostrar separadamente
for (let i = 0; i < arrayNum.length; i++) {
    console.log(arrayNum[i]);
}

//metodo Map
let numeros: number[] = [1,6,4]
let stringNumero: string[] = ['4','6','10']
let dobrados: number[] = numeros.map(numero => numero * 2)
let numeroString: number[] = stringNumero.map(num => Number(num))

console.log(stringNumero);
console.log(numeroString);

let numeros: number[] = [1,6,4]

let somaNumeros: number = numeros.reduce((acumulador,numero) => {
    return acumulador + numero
},0)
*/
